#
# TEMPLATE — paste into conf/mod_sod_world.conf.dist, fill the placeholders. Not
# loaded from templates/ (only conf/*.conf.dist is). Read it in C++ with
#   sConfigMgr->GetOption<T>("SodWorld.<Name>", <default>);
# Ship every key with its documented default.
#

#
#    SodWorld.<Feature>.<Name>
#        Description: <What it controls, in one or two lines.>
#        Default:     <DEFAULT>
#

SodWorld.<Feature>.<Name> = <DEFAULT>
